[project]
name = "blender-dmx"
version = "0.1.0"
description = ""
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "isort>=6.0.0",
    "pip>=25.0",
    "pre-commit>=4.1.0",
    "ruff>=0.9.3",
]
